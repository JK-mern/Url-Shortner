import { Request, Response } from "express"
import uuid from 'uuid'

export const createShortendUrl = (req: Request, res : Response) =>{
    const {originalRoute } = req.body
    let shortendUrl = 
}   



export const getOriginalUrl = () =>{

}